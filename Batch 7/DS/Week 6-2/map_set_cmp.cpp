#include <bits/stdc++.h>

using namespace std;
int main()
{
    priority_queue<string, vector<string>, cmp_class> pq; 
    set<string, cmp_class> pq;
    map<string, int, cmp_class> pq;
    return 0;
}
