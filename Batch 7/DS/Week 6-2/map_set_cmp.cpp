#include <bits/stdc++.h>

using namespace std;
int main()
{
    priority_queue<string, vector<string>, cmp_class> pq; // min heap
    set<string, vector<string>, cmp_class> pq;
    map<string, vector<string>, cmp_class> pq;
    return 0;
}